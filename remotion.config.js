/**
 * Remotion config — used by `npx remotion studio` and `npx remotion render`
 */
import { Config } from '@remotion/cli/config'

Config.setEntryPoint('./remotion/Root.jsx')
Config.setOverwriteOutput(true)
